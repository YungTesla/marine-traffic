# AISStream.io API key (verplicht)
# Verkrijg een gratis key op https://aisstream.io
AISSTREAM_API_KEY=your_api_key_here

# Database pad (optioneel, default: /data/encounters.db in Docker)
# DB_PATH=/data/encounters.db
